//Functions  Anonymous Function



var calcArea = function(width, height){ //defining
    //code the function runs
    var area = width * height;
    return area;
}

var a = calcArea(20, 30); //invoking must happen after defining the function
console.log (a);

